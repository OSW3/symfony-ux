{# ---------------------------------------------- #}
{# - Component : Form Control                   - #}
{# - Part : Label                               - #}
{# ---------------------------------------------- #}
{% extends '@UxComponents/form-widget/tag/label/base.html' %}


{# ---------------------------------------------- #}
{# ---------------------------------------------- #}
{% set _attr = attr|default({}) %}


{# -- Render component attributes --------------- #}
{# ---------------------------------------------- #}

{% set _attr = _add_attribute(_attr, 'class', class) %}
{% set _attr = _add_attribute(_attr, 'for', for) %}
{% set _attr = _add_attribute(_attr, 'hidden', hidden) %}