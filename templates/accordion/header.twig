{# ---------------------------------------------- #}
{# - Component : Brand                          - #}
{# - Part : Header                              - #}
{# ---------------------------------------------- #}
{% extends '@SymfonyUx/accordion/header.html' %}


{# ---------------------------------------------- #}
{# ---------------------------------------------- #}
{% set _attr = attr|default({}) %}


{# -- Render component attributes --------------- #}
{# ---------------------------------------------- #}

{% set _attr = _add_attribute(_attr, 'class', class) %}
{% set _attr = _add_attribute(_attr, 'aria-expanded', open ? 'true' : 'false') %}
{% set _attr = _add_attribute(_attr, 'aria-controls', id) %}
