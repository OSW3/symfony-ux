{# ---------------------------------------------- #}
{# - Component : Brand                          - #}
{# - Part : Text                                - #}
{# ---------------------------------------------- #}
{% extends '@SymfonyUx/brand/text.html' %}

{% set _attr = attr|default({}) %}
{% set _attr = _add_attribute(_attr, 'class', class) %}
{# {% set _attr = _add_attribute(_attr, 'hidden', hidden) %}
{% set _attr = _add_attribute(_attr, 'translate', "no") %} #}