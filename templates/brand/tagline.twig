{# ---------------------------------------------- #}
{# - Component : Brand                          - #}
{# - Part : Tagline                             - #}
{# ---------------------------------------------- #}
{% extends '@UxComponents/brand/tagline.html' %}


{# ---------------------------------------------- #}
{# ---------------------------------------------- #}
{% set _attr = attr|default({}) %}


{# -- Fill in the values ------------------------ #}
{# ---------------------------------------------- #}

{# Set the Class attribute #}
{% set _attr = _add_attribute(_attr, 'class', class) %}

{# Set No translation attribute #}
{% set _attr = _add_attribute(_attr, 'translate', "no") %}